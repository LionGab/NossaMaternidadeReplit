/**
 * Navigation types re-export
 *
 * Central entry to avoid duplicated param list definitions across navigation.
 * Keeps existing source of truth in src/types/navigation.ts while allowing
 * imports from the navigation namespace.
 */
export * from "@/types/navigation";
